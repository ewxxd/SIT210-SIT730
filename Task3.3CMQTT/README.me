This IoT project uses an Arduino Nano 33 IoT and an ultrasonic sensor to automate home lighting via the MQTT protocol. Users can trigger the hallway and bathroom LEDs by performing a "hand wave" or "pat" gesture, which publishes messages to a cloud broker to sync the light states. The system is designed for high responsiveness by utilizing non-blocking timers and the HiveMQ public broker for real-time communication.
